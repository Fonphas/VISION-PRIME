#install pip linux --> sudo apt-get update && sudo apt-get install python3-pip
#install torch 
PyQt5
opencv-python>=4.8
numpy>=1.24
torch>=2.0
ultralytics>=8.0    # for YOLOv8/v9/v10/v11
# yolov5 is loaded via torch.hub — no extra install needed
